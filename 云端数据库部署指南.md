# 🍐 想吃梨儿童艺术启蒙 · 云端数据库与全栈部署指南

本画廊系统已为您构建了**完整的全栈后端架构与云端数据库对接能力**。

您既可以在**本地电脑一键双击启动全栈服务与数据库**，也可以将数据**一键托管到免费的公网云数据库（如 Supabase / PostgreSQL）**，让所有家长在手机端实时同步浏览、发表便签与互动点赞！

---

## 🚀 方式一：本地电脑一键全栈启动（免配置·即开即用）

我们在项目根目录下为您创建了 **`一键启动全栈后端.bat`**：

1. **操作方式**：在文件夹中找到 [`一键启动全栈后端.bat`](file:///c:/Users/Administrator/备份文件（勿删）/Google%20Antigravity/kids-art-gallery/一键启动全栈后端.bat) 双击运行；
2. **运行效果**：
   * 自动在后台启动 Node.js REST API 服务与轻量级持久化数据库；
   * 自动在浏览器中打开 `http://localhost:3000`；
   * 您录入的画作、特展文章、发表的便签和点赞数，**全部实时写入数据库文件 (`db.json`) 永久保存**，刷新页面或重启电脑都不会丢失！

---

## ☁️ 方式二：接入 Supabase 免费云端数据库（强烈推荐·全网多用户实时同步）

如果您准备将网站发给家长群使用，接入 **Supabase 云数据库**是成本最低、最省心的方式（永久免费额度足够成千上万名家长使用）：

### 第一步：创建免费云数据库
1. 访问 [Supabase 官网 (supabase.com)](https://supabase.com) 并免费注册登录；
2. 点击 **"New Project"**（新建项目），输入项目名称（如 `pear-kids-art`）并设置数据库密码；
3. 选择区域为 **`Tokyo (东京)`** 或 **`Singapore (新加坡)`**，点击创建（约等待 1 分钟）。

### 第二步：一键导入数据库表与初始数据
1. 在 Supabase 左侧导航栏点击 **"SQL Editor"**（SQL 编辑器）；
2. 点击 **"New query"**，打开我们项目里的 [`schema.sql`](file:///c:/Users/Administrator/备份文件（勿删）/Google%20Antigravity/kids-art-gallery/schema.sql) 文件；
3. 将 `schema.sql` 里的全部内容**复制并粘贴进 SQL 编辑器**；
4. 点击右下角的 **"Run"** 绿色按钮；
5. 🎉 **大功告成！** 系统所需的 5 张核心表（`artworks`, `students`, `thematic_exhibitions`, `sticky_notes`, `admin_users`）以及全部初始画作和特展数据已 100% 自动创建并导入完毕！

### 第三步：在前端中填入您的专属云密钥
1. 在 Supabase 仪表盘左下角点击 **"Project Settings" -> "API"**；
2. 复制两项信息：
   * **Project URL**（项目地址，如 `https://xyz.supabase.co`）
   * **Project API keys: `anon public`**（公共匿名密钥）
3. 打开本项目中的 [`cloudConfig.js`](file:///c:/Users/Administrator/备份文件（勿删）/Google%20Antigravity/kids-art-gallery/cloudConfig.js)，将第一段的配置替换为您的信息并将 `enabled` 改为 `true`：
   ```javascript
   supabase: {
     enabled: true, // 开启云端直连
     url: "https://你的项目ID.supabase.co",
     anonKey: "你的anon公钥..."
   }
   ```
4. 保存后，您的网站就正式与公网云端数据库打通！

---

## 🌐 方式三：将画廊免费发布到公网域名（给家长手机访问）

有了云数据库后，您可以把前端网页免费托管到公网上：

1. **Vercel / Netlify（全球免费加速）**：
   * 访问 [Netlify.com](https://app.netlify.com/drop)；
   * 直接把 `kids-art-gallery` 文件夹拖拽进去；
   * 3 秒钟即可生成一个所有人都能打开的公网网址（并自动支持手机微信打开、支持自定义域名）。
2. **腾讯云 Web 托管 / 阿里云 OSS 静态网站**：
   * 上传整个文件夹中的所有文件，即可绑定您自己的国内备案域名。

---

## 📁 核心工程与后端文件清单

| 文件名 | 作用说明 |
| :--- | :--- |
| [`schema.sql`](file:///c:/Users/Administrator/备份文件（勿删）/Google%20Antigravity/kids-art-gallery/schema.sql) | 云端 PostgreSQL / MySQL 数据库建表与初始数据导入全量 SQL 脚本 |
| [`cloudConfig.js`](file:///c:/Users/Administrator/备份文件（勿删）/Google%20Antigravity/kids-art-gallery/cloudConfig.js) | 前端与云端数据库 / REST API 异步通信服务层（含离线平滑降级） |
| [`server.js`](file:///c:/Users/Administrator/备份文件（勿删）/Google%20Antigravity/kids-art-gallery/server.js) | 官方 Node.js RESTful API 后端服务程序 |
| [`package.json`](file:///c:/Users/Administrator/备份文件（勿删）/Google%20Antigravity/kids-art-gallery/package.json) | 后端依赖与启动脚本配置文件 |
| [`.env.example`](file:///c:/Users/Administrator/备份文件（勿删）/Google%20Antigravity/kids-art-gallery/.env.example) | 云数据库连接字符串与密钥环境变量配置模板 |
| [`一键启动全栈后端.bat`](file:///c:/Users/Administrator/备份文件（勿删）/Google%20Antigravity/kids-art-gallery/一键启动全栈后端.bat) | Windows 电脑端双击一键启动脚本 |
| [`云端数据库部署指南.md`](file:///c:/Users/Administrator/备份文件（勿删）/Google%20Antigravity/kids-art-gallery/云端数据库部署指南.md) | 本部署教程指南文档 |
